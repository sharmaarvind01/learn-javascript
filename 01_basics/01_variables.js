const userId = 123455
let userEmail = "arvind@ars.com"
var userPass = "arvind"
city  = "mumbai"

/**
 * here are some examples of variables in javasctipt that you can use and you should
 * avoid to use
 * where data type const where you are not allowd to resign value
 * and data type let is what you can use as block level declaration of value and you can reassign it
 * and var is a global scope variable which you should avaoid to use it 
 * not declared data type which you should avoid useing it this is not good for storing data
 * 
 * */
// userId = 23245
console.log(city)
console.table([userId, userEmail, userPass, city])